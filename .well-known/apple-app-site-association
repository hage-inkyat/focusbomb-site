{
  "_comment": "EXTERNAL_BLOCKER: appIDs below is a placeholder, not valid AASA syntax -- Apple fetches this file as literal JSON, so '$(...)' will NOT be substituted. A human (or a deploy-time build script fed by an environment variable) must replace 'REPLACE_WITH_TEAM_ID.REPLACE_WITH_BUNDLE_ID' with the real '<AppleTeamID>.<BundleID>' string before this file is served in production. See docs/HUMAN_ACTION_REQUIRED.md.",
  "applinks": {
    "details": [
      {
        "appIDs": ["REPLACE_WITH_TEAM_ID.REPLACE_WITH_BUNDLE_ID"],
        "components": [
          { "/": "/challenge/*", "comment": "Challenge deep link" },
          { "/": "/creator/*", "comment": "Creator deep link" },
          { "/": "/result/*", "comment": "Public result deep link" },
          { "/": "/download", "comment": "App Store redirect" }
        ]
      }
    ]
  }
}
